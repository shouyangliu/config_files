# i3 config file (v4)
# dwm-style layout: 左边主窗口，右边从上到下平铺

set $mod Mod4
font pango:monospace 10

# 默认布局
workspace_layout default

# 启动项
exec_always --no-startup-id feh --randomize --bg-fill ~/Downloads/wallpaper/
exec_always --no-startup-id picom --corner-radius=10
exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id dunst &

# 窗口样式
default_border pixel 1
default_floating_border pixel 1
for_window [class="^.*"] floating disable

# gaps
gaps inner 5
gaps outer 5
smart_borders on

# 颜色主题
client.focused          #005577 #005577 #eeeeee #00FF7F
client.focused_inactive #444444 #444444 #bbbbbb #444444
client.unfocused       #222222 #222222 #bbbbbb #444444
client.urgent          #f7768e #f7768e #ffffff #f7768e
client.background     #222222

floating_modifier $mod
tiling_drag modifier titlebar

# 终端
bindsym $mod+Return exec st
bindsym $mod+Shift+Return exec st

# 关闭窗口
bindsym $mod+Shift+c kill

# 启动器
bindsym $mod+p exec --no-startup-id rofi -show drun -theme ~/.config/rofi/catppuccin.rasi -font "monospace 12"

# 壁纸
bindsym $mod+w exec --no-startup-id feh --randomize --bg-fill ~/Downloads/wallpaper/

# 截图
bindsym Print exec --no-startup-id scrot

# 锁屏
bindsym $mod+Shift+x exec --no-startup-id i3lock

# 浏览器
bindsym $mod+Shift+b exec --no-startup-id firefox

# 音量控制
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10%
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10%
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle

# 焦点切换 (h j k l)
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# 移动窗口
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# 分割窗口
bindsym $mod+h split h
bindsym $mod+v split v

# gaps 调整
bindsym $mod+minus gaps inner current minus 5
bindsym $mod+plus gaps inner current plus 5
bindsym $mod+Shift+minus gaps inner current set 0
bindsym $mod+Shift+plus gaps inner current toggle

# 全屏
bindsym $mod+f fullscreen toggle

# 布局
bindsym $mod+t layout stacking
bindsym $mod+g layout toggle split

# 浮动与焦点
bindsym $mod+Shift+space floating toggle
bindsym $mod+space focus mode_toggle
bindsym $mod+a focus parent

# 工作区
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"

bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9

# 重载与退出
bindsym $mod+Shift+r reload
bindsym $mod+Shift+Mod1+r restart
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'exit i3?' -B 'Yes' 'i3-msg exit'"

# 调整大小模式
mode "resize" {
    bindsym j resize shrink width 10 px or 10 ppt
    bindsym k resize grow height 10 px or 10 ppt
    bindsym l resize shrink height 10 px or 10 ppt
    bindsym semicolon resize grow width 10 px or 10 ppt
    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# 状态栏
bar {
    status_command i3status
    colors {
        background #222222
        statusline #eeeeee
        separator #444444
        focused_workspace  #005577 #005577 #eeeeee
        active_workspace   #444444 #444444 #eeeeee
        inactive_workspace #222222 #222222 #bbbbbb
        urgent_workspace   #f7768e #f7768e #ffffff
        binding_mode       #f7768e #f7768e #ffffff
    }
    font pango:monospace 10
}
